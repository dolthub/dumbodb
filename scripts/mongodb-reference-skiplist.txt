# MongoDB Reference Suite — Known-Failing Tests Skip List
#
# Tests listed here are expected to fail and are excluded from the "unexpected
# failures" check in the mongodb-reference GitHub Actions workflow.
#
# Format: one test name per line (base test name only, no subtest paths).
# Lines starting with # and blank lines are ignored.
#
# Maintenance:
#   - Add tests here when a failure is confirmed to be structural (not a bug).
#   - Remove tests here when the underlying issue is fixed.
#   - Prefer narrow entries (specific test names) over broad exclusions.
#
# --- Category: Auth / SASL infrastructure (structural, version-independent) ---
#
# These tests require MongoDB to be running with real auth users and --auth
# enabled. The CI service container uses the default mongo:7.0.8 image without
# pre-created users, so the SASL handshake fails before the test can exercise
# its intended error path. These failures are not dongo bugs — they are a test
# suite infrastructure limitation.
#
# Reference: docs/ferretdb-test-analysis.md, Category B
#
TestHelloIsMasterOpQuerySpeculative
TestHelloOpQuerySASLSupportedMechs
TestHelloSpeculative
TestLogoutCommandAuthenticatedUser
TestOpQuery
TestOpQueryHello
TestOpQueryIsMaster
TestOpQueryIsMasterHelloOk
TestSASLContinueErrors
TestSASLContinueOpQueryErrors
